apiVersion: v1
kind: Service
metadata:
  name: {{ include "mgmt-web-portal.fullname" . }}
  labels:
    {{- include "mgmt-web-portal.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mgmt-web-portal.selectorLabels" . | nindent 4 }}
