apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mgmt-web-portal.fullname" . }}-test-connection"
  labels:
    {{- include "mgmt-web-portal.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mgmt-web-portal.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
